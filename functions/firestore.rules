rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // All reads/writes go through the Admin SDK inside Cloud Functions,
    // which bypasses these rules entirely. Direct client access is never
    // expected, so deny everything.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
